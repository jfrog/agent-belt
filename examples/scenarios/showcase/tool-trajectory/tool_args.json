{
  "name": "tool_args",
  "description": "Demonstrates tool_args_contain. Asserts that when the named tool is invoked, its arguments contain the given substrings. Useful for checking the agent reads the right file or greps for the right pattern.",
  "tags": ["showcase", "tool-trajectory", "single-turn", "tool-args", "real-runnable"],
  "turns": [
    {
      "message": "Read the file pyproject.toml in the current directory and tell me the project name. Use the Read tool with the exact path 'pyproject.toml'.",
      "expect": {
        "no_errors": true,
        "has_reply": true,
        "tools_invoked_any": [["Read"]],
        "tool_args_contain": {
          "Read": {"path": "pyproject.toml"}
        }
      }
    }
  ]
}
